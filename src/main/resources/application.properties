spring.application.name=finsavior-monolith

server.servlet.context-path=/api

# Bound multipart uploads (profile images, AI document/audio) to mitigate memory-exhaustion DoS.
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB
