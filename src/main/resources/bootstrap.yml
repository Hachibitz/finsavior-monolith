spring:
  application:
    name: ${finsavior.active-profile}
  cloud:
    config:
      uri: http://localhost:8084
      enabled: true
      fail-fast: true
      name: ${finsavior.active-profile}
      profile: ''
      label: finsavior-monolith

server:
  port: 8085