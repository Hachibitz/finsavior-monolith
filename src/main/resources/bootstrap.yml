spring:
  application:
    name: dev
  cloud:
    config:
      uri: http://localhost:8084
      enabled: true
      fail-fast: true
      name: dev
      profile: ''
      label: finsavior-monolith

server:
  port: 8085